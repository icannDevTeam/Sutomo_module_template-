<div class="sp-ai-summary">
    <div class="sp-ai-summary__head">
        <span class="sp-ai-summary__badge">🤖 AI Summary (templated)</span>
        <span class="sp-ai-summary__hint">Live LLM call deferred — replace with real generation when API key is wired.</span>
    </div>
    <p class="sp-ai-summary__body">{{ $summary ?? '—' }}</p>
</div>
